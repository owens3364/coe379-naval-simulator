# Utilities

This directory contains various scripts for plotting stuff, etc. To set up, run `uv sync` (may require installing [uv](https://docs.astral.sh/uv/) first) and then any script in this directory can be run with `uv run <script_name>.py` unless otherwise mentioned.
