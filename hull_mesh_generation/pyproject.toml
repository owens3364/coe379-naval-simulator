[project]
name = "hull-mesh-generation"
version = "0.0.0"
description = "Hull mesh generation code for the naval simulator"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "capytaine>=2.3.1",
    "pyqt6>=6.11.0",
    "trimesh>=4.12.1",
    "vispy>=0.16.1",
]
